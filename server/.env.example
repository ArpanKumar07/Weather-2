PORT=5000
JWT_SECRET=mausam360_super_secret_jwt_key_2026

# Database configuration (Defaults to zero-config SQLite if DB_TYPE is not mysql or MySQL unreachable)
DB_TYPE=sqlite
DB_FILE=./mausam360.db

# MySQL Settings (for Render deployment or local MySQL instance)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=mausam360_db
